global:
  hypervisor:
    type: invalid_type  # Invalid hypervisor type
    host: pve.homelab.local
    # Missing username field
  
  backup:
    enabled: true
    root: relative/path  # Invalid - must be absolute
    retention_days: -5   # Invalid - must be positive

services:
  - name: test_service
    type: invalid_service_type  # Invalid service type
    enabled: true
  
  # Missing vmid for lxc type
  - name: incomplete_lxc
    type: lxc
    node: pve
    # Missing vmid - should fail validation
  
  # Missing container_name for docker type
  - name: incomplete_docker
    type: docker
    # Missing container_name - should fail validation
